# MOSt V16.12 test build

Corrects the reconciliation cursor index tag to the valid six-character name feesvc in both INDEX and SEEK. The final pass synchronizes every matching server fee row and reports the synchronized count.

Includes all cumulative V16.10 changes.
