# MOSt V16.63.6 Premium Claims Root Form Fix

Build `1.7.670`.

The corrected `modify_claims.scx/.sct` files are installed directly in the MOSt root folder because that is the path reported by the runtime error. The form uses the configured shared data path before opening `submited.dbf`.

Installer: `MOSt_V16.63.6_PREMIUM_CLAIMS_ROOT_XP.zip`
