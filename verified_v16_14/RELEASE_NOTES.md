# MOSt V16.14 test build

Suppresses the two visible server database path lines when opening Fee Codes by setting TALK OFF before the fees and claims cursor database paths are assigned. The form continues to read the configured server database.

Includes cumulative V16.13 fixes.
