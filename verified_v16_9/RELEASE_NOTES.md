# MOSt V16.9 test build

Startup now reads version information from the executable actually being run. Descriptively named test builds no longer fail because a separate MOSt.exe is absent. File version is 1.7.611.

Includes the cumulative V16.8 fee updater repairs and update-result summary.
